import { createContext } from 'react';

// export const userContext = createContext({ user: null, setUser: () => { } });
export const userContext = createContext({ user: 'Kasey', setUser: () => { } });